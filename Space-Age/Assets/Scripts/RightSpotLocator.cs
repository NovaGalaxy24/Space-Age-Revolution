using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RightSpotLocator : MonoBehaviour
{
   

    // Start is called before the first frame update
    void OnCollisionEnter()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
